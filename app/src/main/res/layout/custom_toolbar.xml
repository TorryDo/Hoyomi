<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/white"


    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/customToolbarIconBack"
        android:layout_width="30dp"
        android:layout_height="30dp"

        android:src="@drawable/temp_back_icon"
        android:layout_marginStart="15dp"
        android:padding="5dp"

        app:layout_constraintTop_toTopOf="@id/customToolbarTxtTitle"
        app:layout_constraintBottom_toBottomOf="@id/customToolbarTxtTitle"
        app:layout_constraintStart_toStartOf="parent"

        />

    <TextView
        android:id="@+id/customToolbarTxtTitle"
        android:layout_width="0dp"
        android:layout_height="0dp"

        android:text="@string/app_name"
        android:textColor="@color/black"
        android:gravity="center"
        android:textSize="25dp"
        android:textStyle="bold"

        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />

    <ImageView
        android:id="@+id/customToolbarMenu"
        android:layout_width="30dp"
        android:layout_height="30dp"

        android:src="@drawable/ic_menu"
        android:layout_marginEnd="15dp"
        android:padding="2dp"


        app:layout_constraintTop_toTopOf="@id/customToolbarIconBack"
        app:layout_constraintEnd_toEndOf="parent"

        />

</androidx.constraintlayout.widget.ConstraintLayout>